import { ColorScheme } from "@/hooks/useTheme";
import { StyleSheet } from "react-native";

export const createAdvancedImageryStyle = (colors: ColorScheme) => {
    const styleSheet = StyleSheet.create({});

    return styleSheet
}
